# Same semantics as web NEXT_PUBLIC_API_BASE: origin + /api, no trailing slash
# Example: http://10.0.2.2:8000/api (Android emulator → host loopback)
EXPO_PUBLIC_API_BASE=http://127.0.0.1:8000/api

# Mapbox token (copy from web NEXT_PUBLIC_MAPBOX_TOKEN)
EXPO_PUBLIC_MAPBOX_TOKEN=
